micronaut:
  http:
    services:
      exchange-client:
        url: 'will-be-replaced-in-test'
        read-timeout: 1m

datasources:
  default:
    url: 'will-be-replaced-in-test'

jpa:
  default:
    properties:
      hibernate.hbm2ddl.auto: none
